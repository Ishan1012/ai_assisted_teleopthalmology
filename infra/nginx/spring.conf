server {
    listen 80;
    server_name api.clearsighteye.app;

    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Commented-out SSL / HTTPS server block (Certbot auto-configuration template format)
# server {
#     listen 443 ssl;
#     server_name api.clearsighteye.app;
#
#     ssl_certificate /etc/letsencrypt/live/api.clearsighteye.app/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.clearsighteye.app/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8081;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
